# Server-side only. Never prefix this key with VITE_ and never commit .env.
AI_API_KEY=
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-5
# Use "responses" for OpenAI. Use "chat" for providers exposing /chat/completions.
AI_API_STYLE=responses
PORT=8787
