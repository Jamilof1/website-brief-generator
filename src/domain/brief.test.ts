import{describe,expect,it}from'vitest';import{buildWebsiteBrief,type WebsiteInput}from'./brief';const x:WebsiteInput={business:'Northstar',offer:'AI workflow design',audience:'B2B teams',goal:'book a discovery call',proof:'Three projects',tone:'Confident',features:['Knowledge hub'],content:'Client logos',competitors:''};describe('website brief',()=>{it('builds a useful core sitemap',()=>expect(buildWebsiteBrief(x).pages.map(p=>p.name)).toContain('Services'));it('adds conditional knowledge architecture',()=>expect(buildWebsiteBrief(x).pages.map(p=>p.name)).toContain('Insights'));});
